<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enjoy.api.mapper.ChnLogMapper">

    <insert id="insert">
        INSERT INTO CHN_LOG (ID, CHN_ID, USR_ID, OLD_STATUS, NEW_STATUS, REG_DT)
        VALUES (
                   #{id},
                   #{chnId},
                   #{usrId},
                   #{oldStatus},
                   #{newStatus},
                   CURRENT_TIMESTAMP()
               )
    </insert>

</mapper>