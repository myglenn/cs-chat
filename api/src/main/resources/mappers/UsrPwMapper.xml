<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enjoy.api.mapper.UsrPwMapper">

    <insert id="insert">
        INSERT INTO USR_PW (USR_ID, PW, UPD_DT)
        VALUES (#{usrId}, #{pw}, CURRENT_TIMESTAMP())
    </insert>

    <update id="update">
        UPDATE USR_PW
        SET PW     = #{pw},
            UPD_DT = CURRENT_TIMESTAMP()
        WHERE USR_ID = #{usrId}
    </update>

</mapper>